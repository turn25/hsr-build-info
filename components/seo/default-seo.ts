import type { DefaultSeoProps } from 'next-seo';

export const defaultSEOConfig: DefaultSeoProps = {
  defaultTitle: 'Honkai Star Rail Build Info',
  titleTemplate: '%s - HSR Build Info',
  description: 'HSR Build Info',
};
